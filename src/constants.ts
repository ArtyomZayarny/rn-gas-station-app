export enum IconType {
  TICKET = 'ticket',
  USER = 'user',
  PRICE = 'price',
  SALES = 'sales',
  MAP = 'map',
}

export const SMS_CODE = '6374';

export const enum RegistrationStep {
  PHONE = 'phone',
  CODE = 'code',
  INFO = 'info',
  DATE = 'date',
}
